# Xcode
build/
DerivedData/
*.xcodeproj
*.xcworkspace
xcuserdata/
*.xcuserstate

# XcodeGen 生成文件：project.yml 的 info.properties / entitlements.properties
# 每次 xcodegen generate 都会重新生成并覆盖这些文件（设计上以 project.yml 为
# 唯一事实源）。不入库防止：① 误提交后改 properties 被 generate 静默覆盖，
# 出现"改了不生效"；② generate 前误读磁盘上的过期副本
IPAManager.entitlements
Sources/Info.plist
Sources/ShareExtension/Info.plist
Sources/ShareExtension/ActionInfo.plist
Sources/ShareExtension/IPAManagerShare.entitlements
Sources/ShareExtension/IPAManagerAction.entitlements

# Swift Package Manager
.build/
.swiftpm/

# macOS
.DS_Store

# IDE
.idea/
.vscode/
*.swp

# 构建产物与证书敏感文件（README 承诺不入库）
Vendor/openssl/
.zsign_cache/
*.p12
*.pfx
*.mobileprovision
*.cer
*.pem
*.key
*.ipa
# 本地括号校验脚本（硬编码 Windows 路径，仅本地使用）
cb.py
# 数据恢复调试目录（analyze.js/fixlog.js/jsonl 原始会话数据，体积 6GB+，仅本地排查用）
_dsh_recover/
